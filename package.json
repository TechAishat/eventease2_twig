{
  "name": "ticketmaster-app",
  "version": "1.0.0",
  "description": "TicketMaster Application",
  "scripts": {
    "start": "php -S 0.0.0.0:${PORT:-8000} -t public"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
